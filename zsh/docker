STATUS=$(docker-machine status dinghy)

if [ "$STATUS" = "Stopped" ]; then
  echo "Starting dinghy..."
  docker-machine start dinghy &>/dev/null
fi

STARTUP=`docker-machine env dinghy`
eval ${STARTUP}
